# Copy to deploy.env (gitignored) for a local ./deploy.sh.
# GitHub Actions uses secrets VPS_HOST / VPS_USER / VPS_SSH_KEY instead.
SSH_HOST=hetzner-arv-public
SSH_USER=root
REMOTE_PATH=/var/www/carabetta.xyz/dotsbr
